# Development and verification tools.
# Install after the runtime dependencies:
#   python -m pip install -r requirements.txt -r requirements-dev.txt

pytest>=8,<9
pytest-asyncio>=0.23,<1
httpx>=0.27,<0.28
ruff>=0.8,<1
bandit>=1.7,<2
pip-audit>=2.7,<3
